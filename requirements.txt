bqplot
ipycanvas>=0.9.1

# (Allows for keyboard/mouse events to be implemented and used)
ipyevents>=2.0.1

# Allows for interactive map widgets to be implemented
# ipyleaflet

# (Plot handling and interaction)
ipympl>=0.8.2

# (Allows for interaction/manipulation of HTML widgets)
# ipywidgets>=8.0.0,<9

# (Interactive web environment - essential)
jupyterlab~=3.4.7

# (Allows for ability to create flowcharts, diagrams etc.)
jupyterlab-drawio

# (Language packs (French and Chinese))
# jupyterlab-language-pack-fr-FR
# jupyterlab-language-pack-zh-CN

# (Renderer for Fasta data and files - text-based format for representing either nucleotide sequences or amino acid (protein) sequences))
# jupyterlab-fasta>=3,<4

# (Allows for plotting of GeoJSON data - format for encoding a variety of geographic data structures)
# jupyterlab-geojson>=3,<4

#(Provides a tutorial in using JupyterLab) 
jupyterlab-tour 

# (Just themes, not necessary but nice for aesthethics)
jupyterlab_miami_nights 
theme-darcula

# (JupyterLab distribution - essential)
jupyterlite==0.1.0b13

# (Templates which aren't needed in this instance)
jupyterlite-p5-kernel==0.1.0a12
jupyterlite-xeus-lua==0.3.1
jupyterlite-xeus-wren==0.2.1
jupyterlite-xeus-sqlite==0.2.1

# Provides 40 plot category types to display data
plotly>=5,<6

